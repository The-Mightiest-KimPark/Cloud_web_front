import React, {Components} from 'react';

class TodoItemList extends Components {
    render() {
        const {todos, onToggle, onRemove} = this.props;

        return (
            <div>

            </div>
        );
    }
}

export default TodoItemList;